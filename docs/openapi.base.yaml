openapi: 3.1.0
servers:
  - url: /grpc

info:
  title: TrevStack API
  version: 1.0.1
  description: API for TrevStack
  contact:
    name: Trev
    email: spam@trev.xyz

components:
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: stack_session

security:
  - sessionCookie: []
