#:schema https://www.schemastore.org/treefmt.json

[formatter.gofmt]
command = "gofmt"
includes = ["*.go"]

[formatter.oxfmt]
command = "oxfmt"
options = ["--write"]
includes = ["*.ts", "*.tsx", "*.html", "*.json", "*.yaml", "*.toml", "*.md"]

[formatter.buf]
command = "buf"
options = ["format", "--write"]
includes = ["*.proto"]
no-positional-arg-support = true

[formatter.sqlfluff]
command = "sqlfluff"
options = ["format"]
includes = ["*.sql"]

[formatter.nixfmt]
command = "nixfmt"
includes = ["*.nix"]
